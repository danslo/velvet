import TextField from '@mui/material/TextField';
import {FieldProps} from "../Field";

const Text = ({field}: FieldProps) => {
    return <TextField variant="standard" sx={{marginTop: "-18px"}}/>
}

export default Text;
