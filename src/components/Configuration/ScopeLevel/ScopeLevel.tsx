import {FunctionComponent} from "react";
import {Scope} from "../../../types";

type ScopeLevelProps = {
    level: number
    scope: Scope
}

const ScopeLevel: FunctionComponent<ScopeLevelProps> = ({children, level, scope}) => (
    <>

    </>
)

export default ScopeLevel;
