query getConfigurationSection($section: String!) {
    configurationSection(section: $section){
        label
        fields {
            label
            type
            comment
            options {
                label
                value
            }
            inherit
            show_inherit
            value
        }
    }
}
